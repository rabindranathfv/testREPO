import { Type } from '@angular/core';
export declare class ColumnFilterService {
    private registeredFilters;
    registerFilter(type: string, component: Type<any>): void;
    getFilter(type: string): Type<any>;
}
